This document outlines the project structure for `ai-doc-gen-i2t6qfb4`, a full-stack application. It aims to provide a clear understanding of the codebase organization, facilitating development, maintenance, and onboarding for new contributors.

## 1. Overview

The `ai-doc-gen-i2t6qfb4` project is structured as a monorepo, separating the application into distinct `back-end` and `front-end